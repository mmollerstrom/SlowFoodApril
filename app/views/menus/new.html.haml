%h2 New Menu Form
= form_for([@user, @restaurant, @menu]) do |f|
  .field
    = f.label :name, "Menu Name"
    %br/
    = f.text_field :name, autofocus: true
  .actions
    = f.submit "Create"
    %p
      = flash[:notice]
