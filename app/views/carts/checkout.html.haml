%h2 Checkout page
%h3 My Cart Items:
%ul
  - @items.each do |item|
    %li Dish: #{item['name']}
    %li Price: #{item['price']}
%h5 Total price: #{@total_price}
= link_to 'Pay order', new_charge_path
